<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!-- FCM listener: receives push payloads on Android. The plugin forwards
             them to Dart via the ios_apn_manager MethodChannel. -->
        <service
            android:name="app.erohub.push.ErohubFcmService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>
</manifest>
